<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="decode2017_MW08.Views.ListViewHasNoHightlightPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    Title="ListViewHasNoHightlight"
    prism:ViewModelLocator.AutowireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ListView HorizontalOptions="FillAndExpand" ItemsSource="{Binding ItemsSource}" />
        <ListView
            Grid.Column="1"
            HorizontalOptions="FillAndExpand"
            ItemsSource="{Binding ItemsSource}">
            <ListView.Effects>
                <Effect x:FactoryMethod="Resolve">
                    <x:Arguments>
                        <x:String>Xamarin.ListViewHasNoHighlightEffect</x:String>
                    </x:Arguments>
                </Effect>
            </ListView.Effects>
        </ListView>
    </Grid>
</ContentPage>